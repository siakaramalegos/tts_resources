<h1>Great Web Dev Resources</h1>

<div class="well">
<p>Filter by:</p>
<p>
  <%= link_to "Show All", resources_path, class: "btn btn-default btn-xs filter" %>
  <% @categories.order(:name).each do |category| %>
    <a href="?category_ids=<%= category.id%>"><button class="btn btn-warning btn-xs filter"><%= category.name %></button></a>
  <% end %>
</p>
</div>

<%= link_to 'New Resource', new_resource_path, :class => "btn btn-default", type: "button" %>


<table class="table">
  <thead>
    <tr>
      <th>Title</th>
      <th>Notes</th>
      <th>Category</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @resources.each do |resource| %>
      <tr>
        <td><%= link_to resource.title, resource.link, target: "_blank", :class => "resource-link" %> <%= link_to fa_icon('thumbs-up', text: resource.upvotes), upvote_resource_path(resource), method: :put %> <%= link_to fa_icon('thumbs-down', text: resource.downvotes), downvote_resource_path(resource), method: :put %></td>
        <td><%= resource.notes %></td>
        <td><%= resource.category.name %></td>
        <td><%= link_to 'Edit', edit_resource_path(resource), :class => "btn btn-default btn-xs" %></td>
        <td><%= link_to 'Delete', resource, method: :delete, data: { confirm: 'Are you sure?' }, :class => "btn btn-default btn-xs" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>


