<% if user_signed_in? %>
  <li>
  <%= link_to(fa_icon('sign-out', text: 'Logout'), destroy_user_session_path, :method => :delete) %>
  </li>
<% else %>
  <li>
  <%= link_to(fa_icon('sign-in', text: 'Login'), new_user_session_path)  %>
  </li>
<% end %>